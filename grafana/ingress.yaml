apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: grafana
spec:
  rules:
    - host: grafana.nuc
      http:
        paths:
          - path: /
            backend:
              serviceName: grafana
              servicePort: 3000
