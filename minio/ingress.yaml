apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: minio
spec:
  rules:
    - host: minio.nuc
      http:
        paths:
          - path: /
            backend:
              serviceName: minio
              servicePort: 9000
            
