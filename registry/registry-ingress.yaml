apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: registry
spec:
  rules:
    - host: registry.nuc
      http:
        paths:
          - path: /
            backend:
              serviceName: registry
              servicePort: 5000
